<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase Tokens - RinglyPro</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            max-width: 600px;
            width: 100%;
            padding: 40px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #1f2937;
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .header p {
            color: #6b7280;
            font-size: 1rem;
        }

        .balance-card {
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            color: white;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            text-align: center;
        }

        .balance-card h3 {
            font-size: 0.9rem;
            opacity: 0.9;
            margin-bottom: 8px;
        }

        .balance-card .amount {
            font-size: 2.5rem;
            font-weight: bold;
        }

        .balance-card .plan-badge {
            display: inline-block;
            background: rgba(255,255,255,0.2);
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            margin-top: 10px;
        }

        .section-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e5e7eb;
        }

        .packages {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .package {
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
        }

        .package:hover {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .package.selected {
            border-color: #3b82f6;
            background: #eff6ff;
        }

        .package .tokens {
            font-size: 1.5rem;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .package .price {
            font-size: 1.2rem;
            color: #3b82f6;
            font-weight: 600;
        }

        .package .minutes {
            font-size: 0.85rem;
            color: #9ca3af;
            margin-top: 4px;
        }

        .divider {
            display: flex;
            align-items: center;
            margin: 30px 0;
            color: #9ca3af;
        }

        .divider::before,
        .divider::after {
            content: '';
            flex: 1;
            height: 1px;
            background: #e5e7eb;
        }

        .divider span {
            padding: 0 15px;
            font-size: 0.9rem;
        }

        .btn {
            width: 100%;
            padding: 16px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-primary {
            background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%);
            color: white;
        }

        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(59, 130, 246, 0.4);
        }

        .btn-primary:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .btn-upgrade {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            margin-bottom: 15px;
        }

        .btn-upgrade:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(16, 185, 129, 0.4);
        }

        .btn-secondary {
            background: #f3f4f6;
            color: #6b7280;
            margin-top: 15px;
        }

        .btn-secondary:hover {
            background: #e5e7eb;
        }

        .success-message {
            background: #d1fae5;
            border: 2px solid #10b981;
            color: #065f46;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: none;
        }

        .success-message.show {
            display: block;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .spinner {
            border: 3px solid #f3f3f3;
            border-top: 3px solid #3b82f6;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
            display: inline-block;
            margin-right: 10px;
        }

        /* Modal Styles */
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.7);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .modal-overlay.show {
            display: flex;
        }

        .modal {
            background: white;
            border-radius: 16px;
            max-width: 800px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
            padding: 30px;
        }

        .modal-header {
            text-align: center;
            margin-bottom: 25px;
        }

        .modal-header h2 {
            color: #1f2937;
            font-size: 1.8rem;
            margin-bottom: 8px;
        }

        .modal-header p {
            color: #6b7280;
        }

        .modal-close {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 2rem;
            color: #9ca3af;
            cursor: pointer;
            background: none;
            border: none;
        }

        .modal-close:hover {
            color: #1f2937;
        }

        .plans-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .plan-card {
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            padding: 25px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
            position: relative;
        }

        .plan-card:hover {
            border-color: #3b82f6;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .plan-card.popular {
            border-color: #3b82f6;
            background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
        }

        .plan-card.popular::before {
            content: 'Most Popular';
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: #3b82f6;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .plan-card .plan-name {
            font-size: 1.3rem;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .plan-card .plan-desc {
            font-size: 0.85rem;
            color: #6b7280;
            margin-bottom: 15px;
        }

        .plan-card .plan-price {
            font-size: 2.5rem;
            font-weight: bold;
            color: #3b82f6;
        }

        .plan-card .plan-period {
            font-size: 0.9rem;
            color: #9ca3af;
            margin-bottom: 15px;
        }

        .plan-card .plan-tokens {
            background: #eff6ff;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 15px;
        }

        .plan-card .plan-tokens .token-amount {
            font-size: 1.5rem;
            font-weight: bold;
            color: #3b82f6;
        }

        .plan-card .plan-tokens .token-label {
            font-size: 0.8rem;
            color: #6b7280;
        }

        .plan-card .plan-minutes {
            font-size: 0.9rem;
            color: #10b981;
            font-weight: 500;
            margin-bottom: 15px;
        }

        .plan-card .plan-features {
            text-align: left;
            font-size: 0.85rem;
            color: #4b5563;
        }

        .plan-card .plan-features li {
            padding: 5px 0;
            list-style: none;
        }

        .plan-card .plan-features li::before {
            content: '✓';
            color: #10b981;
            margin-right: 8px;
            font-weight: bold;
        }

        .plan-card .select-plan-btn {
            width: 100%;
            padding: 12px;
            margin-top: 15px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            background: #3b82f6;
            color: white;
        }

        .plan-card .select-plan-btn:hover {
            background: #2563eb;
        }

        .plan-card.popular .select-plan-btn {
            background: #3b82f6;
        }

        .plan-card.popular .select-plan-btn:hover {
            background: #2563eb;
        }

        /* Subscription Management Section */
        .subscription-section {
            display: none;
            background: #f8fafc;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            padding: 24px;
            margin-bottom: 25px;
        }

        .subscription-section.show {
            display: block;
        }

        .subscription-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
        }

        .subscription-header h3 {
            font-size: 1rem;
            color: #1f2937;
            margin: 0;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }

        .status-badge.active {
            background: #d1fae5;
            color: #065f46;
        }

        .status-badge.canceling {
            background: #fef3c7;
            color: #92400e;
        }

        .subscription-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-bottom: 20px;
        }

        .sub-detail {
            background: white;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #e5e7eb;
        }

        .sub-detail label {
            font-size: 0.75rem;
            color: #6b7280;
            display: block;
            margin-bottom: 4px;
        }

        .sub-detail span {
            font-size: 0.95rem;
            font-weight: 600;
            color: #1f2937;
        }

        .cancel-warning {
            background: #fef3c7;
            border: 1px solid #fbbf24;
            color: #92400e;
            padding: 12px 16px;
            border-radius: 8px;
            font-size: 0.9rem;
            margin-bottom: 12px;
        }

        .btn-cancel {
            width: 100%;
            padding: 12px;
            border: 2px solid #fca5a5;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            background: #fee2e2;
            color: #dc2626;
        }

        .btn-cancel:hover {
            background: #dc2626;
            color: white;
            border-color: #dc2626;
        }

        .btn-cancel:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="/" style="display:inline-flex;align-items:center;gap:6px;color:#3b82f6;text-decoration:none;font-weight:500;font-size:0.9rem;margin-bottom:16px;">
            ← Back to Dashboard
        </a>
        <div class="header">
            <h1>Purchase Tokens</h1>
            <p>Add tokens or upgrade to a subscription plan</p>
        </div>

        <div class="balance-card">
            <h3>Current Balance</h3>
            <div class="amount" id="currentBalance">Loading...</div>
            <div class="plan-badge" id="currentPlan">Free Plan</div>
        </div>

        <!-- Subscription Management Section (hidden by default, shown for paid plans) -->
        <div class="subscription-section" id="subscriptionSection">
            <div class="subscription-header">
                <h3>Your Subscription</h3>
                <span class="status-badge active" id="subStatusBadge">Active</span>
            </div>
            <div class="subscription-details">
                <div class="sub-detail">
                    <label>Plan</label>
                    <span id="subPlanName">—</span>
                </div>
                <div class="sub-detail">
                    <label>Price</label>
                    <span id="subPrice">—</span>
                </div>
                <div class="sub-detail">
                    <label>Monthly Tokens</label>
                    <span id="subTokens">—</span>
                </div>
                <div class="sub-detail">
                    <label>Billing Started</label>
                    <span id="subBillingDate">—</span>
                </div>
            </div>
            <div class="cancel-warning" id="cancelWarning" style="display:none;">
                Cancellation scheduled — your plan stays active until <strong id="cancelDate"></strong>
            </div>
            <button class="btn-cancel" id="cancelBtn" onclick="cancelSubscription()">
                Cancel Subscription
            </button>
        </div>

        <div id="successMessage" class="success-message">
            Payment successful! Tokens added to your account.
        </div>

        <!-- Upgrade Section -->
        <button class="btn btn-upgrade" onclick="showUpgradeModal()">
            Upgrade to Subscription Plan
        </button>

        <div class="section-title">Or Buy One-Time Tokens</div>

        <div class="packages">
            <div class="package" onclick="selectPackage(9, 100)" data-amount="9">
                <div class="tokens">100</div>
                <div class="price">$9</div>
                <div class="minutes">20 min</div>
            </div>
            <div class="package" onclick="selectPackage(23, 250)" data-amount="23">
                <div class="tokens">250</div>
                <div class="price">$23</div>
                <div class="minutes">50 min</div>
            </div>
            <div class="package" onclick="selectPackage(54, 600)" data-amount="54">
                <div class="tokens">600</div>
                <div class="price">$54</div>
                <div class="minutes">120 min</div>
            </div>
            <div class="package" onclick="selectPackage(117, 1300)" data-amount="117">
                <div class="tokens">1,300</div>
                <div class="price">$117</div>
                <div class="minutes">260 min</div>
            </div>
            <div class="package" onclick="selectPackage(252, 2800)" data-amount="252">
                <div class="tokens">2,800</div>
                <div class="price">$252</div>
                <div class="minutes">560 min</div>
            </div>
            <div class="package" onclick="selectPackage(540, 6000)" data-amount="540">
                <div class="tokens">6,000</div>
                <div class="price">$540</div>
                <div class="minutes">1,200 min</div>
            </div>
        </div>

        <div style="text-align: center; color: #6b7280; margin: 20px 0; font-size: 0.9rem;">
            You'll be redirected to secure Stripe checkout
        </div>

        <button id="payButton" class="btn btn-primary" disabled onclick="processPayment()">
            Select a package to continue
        </button>

        <button class="btn btn-secondary" onclick="goBack()">
            Cancel
        </button>
    </div>

    <!-- Upgrade Modal -->
    <div class="modal-overlay" id="upgradeModal">
        <div class="modal">
            <div class="modal-header">
                <h2>Choose Your Plan</h2>
                <p>Get more tokens every month with automatic rollover</p>
            </div>

            <div class="plans-grid">
                <!-- Starter Plan -->
                <div class="plan-card">
                    <div class="plan-name">Starter</div>
                    <div class="plan-desc">For small businesses</div>
                    <div class="plan-price">$180</div>
                    <div class="plan-period">/month</div>
                    <div class="plan-tokens">
                        <div class="token-amount">2,000</div>
                        <div class="token-label">tokens/month</div>
                    </div>
                    <div class="plan-minutes">400 minutes of voice</div>
                    <ul class="plan-features">
                        <li>All services included</li>
                        <li>AI Copilot unlimited</li>
                        <li>Priority support</li>
                        <li>Remaining rollover</li>
                    </ul>
                    <button class="select-plan-btn" onclick="selectPlan('starter')">
                        Select Starter
                    </button>
                </div>

                <!-- Growth Plan (Popular) -->
                <div class="plan-card popular">
                    <div class="plan-name">Growth</div>
                    <div class="plan-desc">For growing businesses</div>
                    <div class="plan-price">$540</div>
                    <div class="plan-period">/month</div>
                    <div class="plan-tokens">
                        <div class="token-amount">6,000</div>
                        <div class="token-label">tokens/month</div>
                    </div>
                    <div class="plan-minutes">1,200 minutes of voice</div>
                    <ul class="plan-features">
                        <li>Everything in Starter</li>
                        <li>Advanced analytics</li>
                        <li>Outbound campaigns</li>
                        <li>Team collaboration</li>
                    </ul>
                    <button class="select-plan-btn" onclick="selectPlan('growth')">
                        Select Growth
                    </button>
                </div>

                <!-- Professional Plan -->
                <div class="plan-card">
                    <div class="plan-name">Professional</div>
                    <div class="plan-desc">For large teams</div>
                    <div class="plan-price">$1,350</div>
                    <div class="plan-period">/month</div>
                    <div class="plan-tokens">
                        <div class="token-amount">15,000</div>
                        <div class="token-label">tokens/month</div>
                    </div>
                    <div class="plan-minutes">3,000 minutes of voice</div>
                    <ul class="plan-features">
                        <li>Everything in Growth</li>
                        <li>Account manager</li>
                        <li>White-label options</li>
                        <li>24/7 priority support</li>
                    </ul>
                    <button class="select-plan-btn" onclick="selectPlan('professional')">
                        Select Professional
                    </button>
                </div>
            </div>

            <button class="btn btn-secondary" onclick="closeUpgradeModal()" style="margin-top: 0;">
                Cancel
            </button>
        </div>
    </div>

    <script>
        let selectedAmount = null;
        let selectedTokens = null;
        let currentPlan = 'free';

        // Initialize on page load
        document.addEventListener('DOMContentLoaded', function() {
            loadBalance();
        });

        async function loadBalance() {
            try {
                const token = localStorage.getItem('token');
                const response = await fetch('/api/tokens/balance', {
                    headers: { 'Authorization': `Bearer ${token}` }
                });

                if (response.ok) {
                    const data = await response.json();
                    const balance = data.balance || 0;
                    currentPlan = data.package || 'free';

                    document.getElementById('currentBalance').textContent = `${balance.toLocaleString()} tokens`;

                    // Update plan badge
                    const planNames = {
                        'free': 'Free Plan',
                        'starter': 'Starter Plan',
                        'growth': 'Growth Plan',
                        'professional': 'Professional Plan'
                    };
                    document.getElementById('currentPlan').textContent = planNames[currentPlan] || 'Free Plan';

                    // Hide upgrade button if already on a paid plan
                    if (currentPlan !== 'free') {
                        const upgradeBtn = document.querySelector('.btn-upgrade');
                        upgradeBtn.textContent = 'Change Subscription Plan';
                    }
                }

                // Also load subscription status for cancel section
                await loadSubscriptionStatus();
            } catch (error) {
                console.error('Error loading balance:', error);
                document.getElementById('currentBalance').textContent = '0 tokens';
            }
        }

        async function loadSubscriptionStatus() {
            try {
                const token = localStorage.getItem('token');
                const response = await fetch('/api/subscription/status', {
                    headers: { 'Authorization': `Bearer ${token}` }
                });

                if (!response.ok) return;

                const data = await response.json();
                const sub = data.subscription;

                if (!sub || !sub.hasActiveSubscription) return;

                // Show subscription section
                const section = document.getElementById('subscriptionSection');
                section.classList.add('show');

                // Populate details
                document.getElementById('subPlanName').textContent = sub.planName + ' Plan';
                document.getElementById('subPrice').textContent = '$' + sub.price + '/month';
                document.getElementById('subTokens').textContent = (sub.monthlyAllocation || 0).toLocaleString();
                document.getElementById('subBillingDate').textContent = sub.billingCycleStart
                    ? new Date(sub.billingCycleStart).toLocaleDateString()
                    : '—';

                // Check if already canceling
                if (sub.status === 'canceled' || sub.status === 'canceling') {
                    document.getElementById('subStatusBadge').textContent = 'Canceling';
                    document.getElementById('subStatusBadge').className = 'status-badge canceling';
                    document.getElementById('cancelBtn').style.display = 'none';
                    document.getElementById('cancelWarning').style.display = 'block';
                    // Try to get cancel date from Stripe
                    document.getElementById('cancelDate').textContent = 'the end of your billing period';
                }
            } catch (error) {
                console.error('Error loading subscription status:', error);
            }
        }

        async function cancelSubscription() {
            const planName = document.getElementById('subPlanName').textContent;

            if (!confirm('Are you sure you want to cancel your ' + planName + '?')) return;
            if (!confirm('Your tokens will remain until the end of your current billing period. Confirm cancellation?')) return;

            const btn = document.getElementById('cancelBtn');
            btn.disabled = true;
            btn.textContent = 'Canceling...';

            try {
                const token = localStorage.getItem('token');
                const response = await fetch('/api/subscription/cancel', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${token}`
                    }
                });

                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.error || 'Failed to cancel subscription');
                }

                // Update UI to show canceling state
                document.getElementById('subStatusBadge').textContent = 'Canceling';
                document.getElementById('subStatusBadge').className = 'status-badge canceling';
                btn.style.display = 'none';

                const cancelDate = data.cancelAt ? new Date(data.cancelAt).toLocaleDateString() : 'the end of your billing period';
                document.getElementById('cancelDate').textContent = cancelDate;
                document.getElementById('cancelWarning').style.display = 'block';

                alert('Subscription canceled. You will keep your tokens until ' + cancelDate + '.');

            } catch (error) {
                console.error('Cancel error:', error);
                alert('Failed to cancel: ' + error.message);
                btn.disabled = false;
                btn.textContent = 'Cancel Subscription';
            }
        }

        function selectPackage(amount, tokens) {
            selectedAmount = amount;
            selectedTokens = tokens;

            document.querySelectorAll('.package').forEach(el => el.classList.remove('selected'));
            document.querySelector(`[data-amount="${amount}"]`).classList.add('selected');

            const btn = document.getElementById('payButton');
            btn.disabled = false;
            btn.textContent = `Pay $${amount}`;
        }

        async function processPayment() {
            if (!selectedAmount) return;

            const btn = document.getElementById('payButton');
            const originalText = btn.textContent;
            btn.disabled = true;
            btn.innerHTML = '<span class="spinner"></span>Creating checkout...';

            try {
                const token = localStorage.getItem('token');

                if (!token) {
                    throw new Error('Please log in to purchase tokens');
                }

                // Create Stripe Checkout Session (hosted page - no Stripe.js needed!)
                const response = await fetch('/api/tokens/create-checkout-session', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${token}`
                    },
                    body: JSON.stringify({
                        amount: selectedAmount,
                        tokens: selectedTokens
                    })
                });

                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ error: 'Unknown error' }));
                    console.error('Checkout session error:', errorData);
                    throw new Error(errorData.error || 'Failed to create checkout session');
                }

                const data = await response.json();
                console.log('Checkout session response:', data);

                if (!data.url) {
                    throw new Error('No checkout URL received from server');
                }

                // Redirect to Stripe Checkout - this is a proper navigation that iOS handles well
                window.location.href = data.url;

            } catch (error) {
                console.error('Payment error:', error);
                alert('Payment failed: ' + error.message);
                btn.disabled = false;
                btn.textContent = originalText;
            }
        }

        function showUpgradeModal() {
            document.getElementById('upgradeModal').classList.add('show');
            document.body.style.overflow = 'hidden';
        }

        function closeUpgradeModal() {
            document.getElementById('upgradeModal').classList.remove('show');
            document.body.style.overflow = '';
        }

        async function selectPlan(plan) {
            const btn = event.target;
            const originalText = btn.textContent;
            btn.disabled = true;
            btn.innerHTML = '<span class="spinner"></span>Loading...';

            try {
                const token = localStorage.getItem('token');

                if (!token) {
                    throw new Error('Please log in to upgrade');
                }

                // Create subscription checkout session
                const response = await fetch('/api/subscription/upgrade', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': `Bearer ${token}`
                    },
                    body: JSON.stringify({ plan })
                });

                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ error: 'Unknown error' }));
                    throw new Error(errorData.error || 'Failed to create subscription');
                }

                const data = await response.json();

                if (!data.url) {
                    throw new Error('No checkout URL received from server');
                }

                // Redirect to Stripe Checkout
                window.location.href = data.url;

            } catch (error) {
                console.error('Upgrade error:', error);
                alert('Upgrade failed: ' + error.message);
                btn.disabled = false;
                btn.textContent = originalText;
            }
        }

        // Close modal when clicking outside
        document.getElementById('upgradeModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeUpgradeModal();
            }
        });

        function goBack() {
            window.location.href = '/';
        }
    </script>
</body>
</html>
