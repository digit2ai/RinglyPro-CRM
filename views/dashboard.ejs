<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RinglyPro CRM Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .dropdown-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            z-index: 50;
            min-width: 200px;
        }
        .dropdown-menu.show {
            display: block;
        }
        .modal {
            display: none;
        }
        .modal.show {
            display: flex;
        }
        .slide-up {
            animation: slideUp 0.3s ease-out;
        }
        @keyframes slideUp {
            from {
                transform: translateY(100px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Header -->
    <header class="bg-blue-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <!-- RinglyPro Logo -->
                    <div class="flex items-center space-x-2">
                        <div class="relative">
                            <img src="https://assets.cdn.filesafe.space/3lSeAHXNU9t09Hhp9oai/media/68a15064d7c434d8cab7644b.png" 
                                 alt="RinglyPro Logo" 
                                 class="w-10 h-10 object-contain bg-white rounded-lg shadow-lg p-1">
                        </div>
                        <div class="text-2xl font-bold">
                            <span class="text-white">Ringly</span><span class="text-blue-200">Pro</span>
                        </div>
                    </div>
                    <div class="text-lg font-medium text-blue-100">CRM Dashboard</div>
                </div>
                <div class="flex items-center space-x-4">
                    <button onclick="refreshData()" class="bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded-full text-sm transition duration-200">
                        <i class="fas fa-sync-alt mr-1"></i> Refresh
                    </button>
                    <button onclick="toggleAllSections()" class="bg-purple-500 hover:bg-purple-600 text-white px-3 py-1 rounded-full text-sm transition duration-200">
                        <i class="fas fa-expand-arrows-alt mr-1"></i> <span id="toggle-all-text">Expand All</span>
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Quick Actions Menu Bar -->
        <div class="bg-blue-700 border-t border-blue-500">
            <div class="container mx-auto px-4 py-2">
                <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-1">
                        <!-- Contacts Dropdown -->
                        <div class="relative" id="contacts-dropdown">
                            <button onclick="toggleDropdown('contacts')" class="flex items-center px-3 py-2 rounded text-sm hover:bg-blue-600 transition duration-200">
                                <i class="fas fa-users mr-2"></i>Contacts
                                <i class="fas fa-chevron-down ml-1 text-xs"></i>
                            </button>
                            <div id="contacts-menu" class="dropdown-menu bg-white text-gray-800 rounded-lg shadow-xl border">
                                <a href="#" onclick="openModal('createContactModal')" class="block px-4 py-2 hover:bg-gray-100 border-b">
                                    <i class="fas fa-plus mr-2 text-green-500"></i>Create Contact
                                </a>
                                <a href="#" onclick="openModal('updateEmailModal')" class="block px-4 py-2 hover:bg-gray-100 border-b">
                                    <i class="fas fa-envelope mr-2 text-blue-500"></i>Update Email
                                </a>
                                <a href="#" onclick="openModal('updatePhoneModal')" class="block px-4 py-2 hover:bg-gray-100 border-b">
                                    <i class="fas fa-phone mr-2 text-purple-500"></i>Update Phone
                                </a>
                                <a href="#" onclick="openModal('showContactModal')" class="block px-4 py-2 hover:bg-gray-100">
                                    <i class="fas fa-eye mr-2 text-orange-500"></i>Show Contact Details
                                </a>
                            </div>
                        </div>

                        <!-- Appointments Dropdown -->
                        <div class="relative" id="appointments-dropdown">
                            <button onclick="toggleDropdown('appointments')" class="flex items-center px-3 py-2 rounded text-sm hover:bg-blue-600 transition duration-200">
                                <i class="fas fa-calendar mr-2"></i>Appointments
                                <i class="fas fa-chevron-down ml-1 text-xs"></i>
                            </button>
                            <div id="appointments-menu" class="dropdown-menu bg-white text-gray-800 rounded-lg shadow-xl border">
                                <a href="#" onclick="openModal('createAppointmentModal')" class="block px-4 py-2 hover:bg-gray-100 border-b">
                                    <i class="fas fa-plus mr-2 text-green-500"></i>Create Appointment
                                </a>
                                <a href="#" onclick="openModal('updateAppointmentModal')" class="block px-4 py-2 hover:bg-gray-100 border-b">
                                    <i class="fas fa-edit mr-2 text-blue-500"></i>Update Appointment
                                </a>
                                <a href="#" onclick="showTodaysAppointments()" class="block px-4 py-2 hover:bg-gray-100">
                                    <i class="fas fa-calendar-day mr-2 text-orange-500"></i>Today's Appointments
                                </a>
                            </div>
                        </div>

                        <!-- Quick Search -->
                        <div class="relative ml-4">
                            <input type="text" id="quick-search" placeholder="Quick search..." 
                                   class="bg-blue-800 text-white placeholder-blue-300 px-3 py-2 rounded text-sm w-64 border border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-300">
                            <i class="fas fa-search absolute right-3 top-2.5 text-blue-300"></i>
                        </div>
                    </div>
                    
                    <div class="flex items-center space-x-2">
                        <div class="bg-green-500 text-white px-3 py-1 rounded-full text-sm">
                            <i class="fas fa-circle text-xs"></i> Online
                        </div>
                        <div class="text-sm text-blue-200" id="current-time"></div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="container mx-auto px-4 py-8">
        <!-- Stats Cards -->
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
            <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-200">
                <div class="flex items-center">
                    <div class="p-3 rounded-full bg-blue-500 text-white">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm text-gray-600">Total Contacts</p>
                        <p class="text-2xl font-semibold" id="total-contacts">127</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-200">
                <div class="flex items-center">
                    <div class="p-3 rounded-full bg-green-500 text-white">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm text-gray-600">Today's Appointments</p>
                        <p class="text-2xl font-semibold" id="todays-appointments">8</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-200">
                <div class="flex items-center">
                    <div class="p-3 rounded-full bg-yellow-500 text-white">
                        <i class="fas fa-phone-volume"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm text-gray-600">Total Calls</p>
                        <p class="text-2xl font-semibold" id="total-calls">342</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-200">
                <div class="flex items-center">
                    <div class="p-3 rounded-full bg-purple-500 text-white">
                        <i class="fas fa-comments"></i>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm text-gray-600">Active Chats</p>
                        <p class="text-2xl font-semibold" id="active-chats">23</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <!-- Recent Contacts -->
            <div class="bg-white rounded-lg shadow-md">
                <div class="p-6 border-b border-gray-200">
                    <div class="flex items-center justify-between">
                        <h2 class="text-xl font-semibold text-gray-800">
                            <i class="fas fa-users mr-2 text-blue-500"></i>Recent Contacts
                        </h2>
                        <button onclick="refreshContacts()" class="text-blue-500 hover:text-blue-700">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                    </div>
                </div>
                <div class="p-6">
                    <div id="recent-contacts" class="space-y-4">
                        <!-- Sample Contact -->
                        <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
                            <div class="flex items-center">
                                <div class="w-10 h-10 bg-blue-500 rounded-full flex items-center justify-center text-white font-semibold">
                                    JD
                                </div>
                                <div class="ml-3">
                                    <p class="font-medium text-gray-900">John Doe</p>
                                    <p class="text-sm text-gray-600">john@example.com • (555) 123-4567</p>
                                </div>
                            </div>
                            <div class="flex space-x-2">
                                <button onclick="editContact('1')" class="text-blue-500 hover:text-blue-700">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button onclick="viewContact('1')" class="text-green-500 hover:text-green-700">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Today's Appointments -->
            <div class="bg-white rounded-lg shadow-md">
                <div class="p-6 border-b border-gray-200">
                    <div class="flex items-center justify-between">
                        <h2 class="text-xl font-semibold text-gray-800">
                            <i class="fas fa-calendar-day mr-2 text-green-500"></i>Today's Appointments
                        </h2>
                        <button onclick="refreshAppointments()" class="text-green-500 hover:text-green-700">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                    </div>
                </div>
                <div class="p-6">
                    <div id="todays-appointments-list" class="space-y-4">
                        <!-- Sample Appointment -->
                        <div class="flex items-center justify-between p-4 bg-green-50 rounded-lg border-l-4 border-green-500">
                            <div>
                                <p class="font-medium text-gray-900">Meeting with Sarah Johnson</p>
                                <p class="text-sm text-gray-600">2:00 PM - 3:00 PM</p>
                                <p class="text-xs text-green-600">Consultation Call</p>
                            </div>
                            <div class="flex space-x-2">
                                <button onclick="editAppointment('1')" class="text-blue-500 hover:text-blue-700">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button onclick="startCall('1')" class="text-green-500 hover:text-green-700">
                                    <i class="fas fa-phone"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Recent Activity -->
            <div class="bg-white rounded-lg shadow-md">
                <div class="p-6 border-b border-gray-200">
                    <h2 class="text-xl font-semibold text-gray-800">
                        <i class="fas fa-history mr-2 text-purple-500"></i>Recent Activity
                    </h2>
                </div>
                <div class="p-6">
                    <div id="recent-activity" class="space-y-4">
                        <!-- Sample Activity -->
                        <div class="flex items-start space-x-3">
                            <div class="w-2 h-2 bg-blue-500 rounded-full mt-2"></div>
                            <div>
                                <p class="text-sm text-gray-900">New contact added: <span class="font-medium">Mike Wilson</span></p>
                                <p class="text-xs text-gray-500">2 minutes ago</p>
                            </div>
                        </div>
                        <div class="flex items-start space-x-3">
                            <div class="w-2 h-2 bg-green-500 rounded-full mt-2"></div>
                            <div>
                                <p class="text-sm text-gray-900">Appointment scheduled with <span class="font-medium">Lisa Davis</span></p>
                                <p class="text-xs text-gray-500">5 minutes ago</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Quick Stats -->
            <div class="bg-white rounded-lg shadow-md">
                <div class="p-6 border-b border-gray-200">
                    <h2 class="text-xl font-semibold text-gray-800">
                        <i class="fas fa-chart-bar mr-2 text-orange-500"></i>Quick Stats
                    </h2>
                </div>
                <div class="p-6">
                    <div class="space-y-4">
                        <div class="flex justify-between items-center">
                            <span class="text-sm text-gray-600">Calls This Week</span>
                            <span class="font-semibold">89</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-sm text-gray-600">Appointments This Week</span>
                            <span class="font-semibold">24</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-sm text-gray-600">New Contacts This Week</span>
                            <span class="font-semibold">15</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-sm text-gray-600">Conversion Rate</span>
                            <span class="font-semibold text-green-600">68%</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modals -->
    
    <!-- Create Contact Modal -->
    <div id="createContactModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-md mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Create New Contact</h3>
                <button onclick="closeModal('createContactModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="createContactForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Full Name</label>
                    <input type="text" id="contact-name" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                    <input type="email" id="contact-email" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Phone</label>
                    <input type="tel" id="contact-phone" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Notes</label>
                    <textarea id="contact-notes" rows="3" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200">
                        <i class="fas fa-plus mr-2"></i>Create Contact
                    </button>
                    <button type="button" onclick="closeModal('createContactModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Update Email Modal -->
    <div id="updateEmailModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-md mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Update Contact Email</h3>
                <button onclick="closeModal('updateEmailModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="updateEmailForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Search Contact</label>
                    <input type="text" id="email-contact-search" placeholder="Enter name or phone..." class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">New Email</label>
                    <input type="email" id="new-email" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200">
                        <i class="fas fa-envelope mr-2"></i>Update Email
                    </button>
                    <button type="button" onclick="closeModal('updateEmailModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Update Phone Modal -->
    <div id="updatePhoneModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-md mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Update Contact Phone</h3>
                <button onclick="closeModal('updatePhoneModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="updatePhoneForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Search Contact</label>
                    <input type="text" id="phone-contact-search" placeholder="Enter name or email..." class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">New Phone</label>
                    <input type="tel" id="new-phone" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200">
                        <i class="fas fa-phone mr-2"></i>Update Phone
                    </button>
                    <button type="button" onclick="closeModal('updatePhoneModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Show Contact Modal -->
    <div id="showContactModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-lg mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Contact Details</h3>
                <button onclick="closeModal('showContactModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="showContactForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Search Contact</label>
                    <input type="text" id="show-contact-search" placeholder="Enter name, email or phone..." class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div id="contact-details" class="hidden mt-4 p-4 bg-gray-50 rounded-lg">
                    <!-- Contact details will be populated here -->
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200">
                        <i class="fas fa-search mr-2"></i>Search Contact
                    </button>
                    <button type="button" onclick="closeModal('showContactModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Create Appointment Modal -->
    <div id="createAppointmentModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-lg mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Create New Appointment</h3>
                <button onclick="closeModal('createAppointmentModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="createAppointmentForm" class="space-y-4">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Contact Name</label>
                        <input type="text" id="appointment-contact" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Phone</label>
                        <input type="tel" id="appointment-phone" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                    </div>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Date</label>
                        <input type="date" id="appointment-date" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Time</label>
                        <input type="time" id="appointment-time" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                    </div>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Type</label>
                    <select id="appointment-type" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                        <option value="">Select appointment type</option>
                        <option value="consultation">Consultation</option>
                        <option value="follow-up">Follow-up</option>
                        <option value="meeting">Meeting</option>
                        <option value="demo">Demo</option>
                    </select>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Notes</label>
                    <textarea id="appointment-notes" rows="3" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-green-600 text-white py-2 px-4 rounded-md hover:bg-green-700 transition duration-200">
                        <i class="fas fa-calendar-plus mr-2"></i>Create Appointment
                    </button>
                    <button type="button" onclick="closeModal('createAppointmentModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Update Appointment Modal -->
    <div id="updateAppointmentModal" class="modal fixed inset-0 bg-black bg-opacity-50 items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl p-6 w-full max-w-lg mx-4 slide-up">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-lg font-semibold text-gray-900">Update Appointment</h3>
                <button onclick="closeModal('updateAppointmentModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="updateAppointmentForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Search Appointment</label>
                    <input type="text" id="appointment-search" placeholder="Enter contact name or appointment ID..." class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                <div id="appointment-update-details" class="hidden space-y-4">
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">New Date</label>
                            <input type="date" id="update-appointment-date" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">New Time</label>
                            <input type="time" id="update-appointment-time" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        </div>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                        <select id="update-appointment-status" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                            <option value="scheduled">Scheduled</option>
                            <option value="completed">Completed</option>
                            <option value="cancelled">Cancelled</option>
                            <option value="rescheduled">Rescheduled</option>
                        </select>
                    </div>
                </div>
                <div class="flex space-x-3 pt-4">
                    <button type="submit" class="flex-1 bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200">
                        <i class="fas fa-edit mr-2"></i>Update Appointment
                    </button>
                    <button type="button" onclick="closeModal('updateAppointmentModal')" class="flex-1 bg-gray-300 text-gray-700 py-2 px-4 rounded-md hover:bg-gray-400 transition duration-200">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // Global variables
        let allSectionsExpanded = false;
        
        // Initialize dashboard
        document.addEventListener('DOMContentLoaded', function() {
            updateTime();
            setInterval(updateTime, 1000);
            loadDashboardData();
            setupEventListeners();
        });

        // Time update
        function updateTime() {
            const now = new Date();
            const timeString = now.toLocaleTimeString('en-US', { 
                hour12: true, 
                hour: 'numeric', 
                minute: '2-digit' 
            });
            document.getElementById('current-time').textContent = timeString;
        }

        // Setup event listeners
        function setupEventListeners() {
            // Quick search
            document.getElementById('quick-search').addEventListener('input', function(e) {
                performQuickSearch(e.target.value);
            });

            // Form submissions
            document.getElementById('createContactForm').addEventListener('submit', handleCreateContact);
            document.getElementById('updateEmailForm').addEventListener('submit', handleUpdateEmail);
            document.getElementById('updatePhoneForm').addEventListener('submit', handleUpdatePhone);
            document.getElementById('showContactForm').addEventListener('submit', handleShowContact);
            document.getElementById('createAppointmentForm').addEventListener('submit', handleCreateAppointment);
            document.getElementById('updateAppointmentForm').addEventListener('submit', handleUpdateAppointment);

            // Close dropdowns when clicking outside
            document.addEventListener('click', function(e) {
                if (!e.target.closest('.relative')) {
                    closeAllDropdowns();
                }
            });
        }

        // Dropdown functionality
        function toggleDropdown(dropdownName) {
            const menu = document.getElementById(dropdownName + '-menu');
            const isOpen = menu.classList.contains('show');
            
            // Close all dropdowns first
            closeAllDropdowns();
            
            // Toggle the clicked dropdown
            if (!isOpen) {
                menu.classList.add('show');
            }
        }

        function closeAllDropdowns() {
            document.querySelectorAll('.dropdown-menu').forEach(menu => {
                menu.classList.remove('show');
            });
        }

        // Modal functionality
        function openModal(modalId) {
            closeAllDropdowns();
            document.getElementById(modalId).classList.add('show');
            document.body.style.overflow = 'hidden';
        }

        function closeModal(modalId) {
            document.getElementById(modalId).classList.remove('show');
            document.body.style.overflow = 'auto';
            
            // Reset form if it exists
            const form = document.querySelector(`#${modalId} form`);
            if (form) {
                form.reset();
                // Hide any dynamic content
                const detailsDiv = form.querySelector('[id*="details"]');
                if (detailsDiv) {
                    detailsDiv.classList.add('hidden');
                }
            }
        }

        // Dashboard data loading
        function loadDashboardData() {
            // Simulate loading data
            console.log('Loading dashboard data...');
            
            // In real implementation, these would be API calls
            loadRecentContacts();
            loadTodaysAppointments();
            loadRecentActivity();
        }

        function loadRecentContacts() {
            // Sample data - replace with actual API call
            const contacts = [
                { id: 1, name: 'John Doe', email: 'john@example.com', phone: '(555) 123-4567' },
                { id: 2, name: 'Sarah Johnson', email: 'sarah@example.com', phone: '(555) 234-5678' },
                { id: 3, name: 'Mike Wilson', email: 'mike@example.com', phone: '(555) 345-6789' }
            ];

            const container = document.getElementById('recent-contacts');
            container.innerHTML = contacts.map(contact => `
                <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-blue-500 rounded-full flex items-center justify-center text-white font-semibold">
                            ${contact.name.split(' ').map(n => n[0]).join('')}
                        </div>
                        <div class="ml-3">
                            <p class="font-medium text-gray-900">${contact.name}</p>
                            <p class="text-sm text-gray-600">${contact.email} • ${contact.phone}</p>
                        </div>
                    </div>
                    <div class="flex space-x-2">
                        <button onclick="editContact('${contact.id}')" class="text-blue-500 hover:text-blue-700">
                            <i class="fas fa-edit"></i>
                        </button>
                        <button onclick="viewContact('${contact.id}')" class="text-green-500 hover:text-green-700">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>
            `).join('');
        }

        function loadTodaysAppointments() {
            // Sample data - replace with actual API call
            const appointments = [
                { id: 1, name: 'Sarah Johnson', time: '2:00 PM - 3:00 PM', type: 'Consultation Call' },
                { id: 2, name: 'Mike Wilson', time: '3:30 PM - 4:30 PM', type: 'Follow-up Meeting' },
                { id: 3, name: 'Lisa Davis', time: '5:00 PM - 6:00 PM', type: 'Demo Session' }
            ];

            const container = document.getElementById('todays-appointments-list');
            container.innerHTML = appointments.map(appointment => `
                <div class="flex items-center justify-between p-4 bg-green-50 rounded-lg border-l-4 border-green-500">
                    <div>
                        <p class="font-medium text-gray-900">Meeting with ${appointment.name}</p>
                        <p class="text-sm text-gray-600">${appointment.time}</p>
                        <p class="text-xs text-green-600">${appointment.type}</p>
                    </div>
                    <div class="flex space-x-2">
                        <button onclick="editAppointment('${appointment.id}')" class="text-blue-500 hover:text-blue-700">
                            <i class="fas fa-edit"></i>
                        </button>
                        <button onclick="startCall('${appointment.id}')" class="text-green-500 hover:text-green-700">
                            <i class="fas fa-phone"></i>
                        </button>
                    </div>
                </div>
            `).join('');
        }

        function loadRecentActivity() {
            // Sample data - replace with actual API call
            const activities = [
                { type: 'contact', message: 'New contact added: Mike Wilson', time: '2 minutes ago', color: 'blue' },
                { type: 'appointment', message: 'Appointment scheduled with Lisa Davis', time: '5 minutes ago', color: 'green' },
                { type: 'call', message: 'Call completed with John Doe', time: '15 minutes ago', color: 'purple' },
                { type: 'email', message: 'Email sent to Sarah Johnson', time: '30 minutes ago', color: 'orange' }
            ];

            const container = document.getElementById('recent-activity');
            container.innerHTML = activities.map(activity => `
                <div class="flex items-start space-x-3">
                    <div class="w-2 h-2 bg-${activity.color}-500 rounded-full mt-2"></div>
                    <div>
                        <p class="text-sm text-gray-900">${activity.message}</p>
                        <p class="text-xs text-gray-500">${activity.time}</p>
                    </div>
                </div>
            `).join('');
        }

        // Form handlers
        function handleCreateContact(e) {
            e.preventDefault();
            
            const contactData = {
                name: document.getElementById('contact-name').value,
                email: document.getElementById('contact-email').value,
                phone: document.getElementById('contact-phone').value,
                notes: document.getElementById('contact-notes').value
            };

            console.log('Creating contact:', contactData);
            
            // Simulate API call
            setTimeout(() => {
                showNotification('Contact created successfully!', 'success');
                closeModal('createContactModal');
                refreshContacts();
            }, 1000);
        }

        function handleUpdateEmail(e) {
            e.preventDefault();
            
            const updateData = {
                search: document.getElementById('email-contact-search').value,
                newEmail: document.getElementById('new-email').value
            };

            console.log('Updating email:', updateData);
            
            // Simulate API call
            setTimeout(() => {
                showNotification('Email updated successfully!', 'success');
                closeModal('updateEmailModal');
                refreshContacts();
            }, 1000);
        }

        function handleUpdatePhone(e) {
            e.preventDefault();
            
            const updateData = {
                search: document.getElementById('phone-contact-search').value,
                newPhone: document.getElementById('new-phone').value
            };

            console.log('Updating phone:', updateData);
            
            // Simulate API call
            setTimeout(() => {
                showNotification('Phone updated successfully!', 'success');
                closeModal('updatePhoneModal');
                refreshContacts();
            }, 1000);
        }

        function handleShowContact(e) {
            e.preventDefault();
            
            const searchTerm = document.getElementById('show-contact-search').value;
            
            // Simulate finding contact
            const mockContact = {
                name: 'John Doe',
                email: 'john@example.com',
                phone: '(555) 123-4567',
                notes: 'VIP client, prefers morning appointments',
                created: '2024-01-15',
                lastContact: '2024-08-10'
            };

            const detailsDiv = document.getElementById('contact-details');
            detailsDiv.innerHTML = `
                <div class="space-y-3">
                    <div class="flex items-center space-x-3">
                        <div class="w-12 h-12 bg-blue-500 rounded-full flex items-center justify-center text-white font-semibold text-lg">
                            ${mockContact.name.split(' ').map(n => n[0]).join('')}
                        </div>
                        <div>
                            <h4 class="font-semibold text-lg">${mockContact.name}</h4>
                            <p class="text-sm text-gray-600">Contact since ${mockContact.created}</p>
                        </div>
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <p class="text-sm font-medium text-gray-700">Email</p>
                            <p class="text-sm text-gray-900">${mockContact.email}</p>
                        </div>
                        <div>
                            <p class="text-sm font-medium text-gray-700">Phone</p>
                            <p class="text-sm text-gray-900">${mockContact.phone}</p>
                        </div>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-700">Notes</p>
                        <p class="text-sm text-gray-900">${mockContact.notes}</p>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-700">Last Contact</p>
                        <p class="text-sm text-gray-900">${mockContact.lastContact}</p>
                    </div>
                </div>
            `;
            detailsDiv.classList.remove('hidden');
        }

        function handleCreateAppointment(e) {
            e.preventDefault();
            
            const appointmentData = {
                contact: document.getElementById('appointment-contact').value,
                phone: document.getElementById('appointment-phone').value,
                date: document.getElementById('appointment-date').value,
                time: document.getElementById('appointment-time').value,
                type: document.getElementById('appointment-type').value,
                notes: document.getElementById('appointment-notes').value
            };

            console.log('Creating appointment:', appointmentData);
            
            // Simulate API call
            setTimeout(() => {
                showNotification('Appointment created successfully!', 'success');
                closeModal('createAppointmentModal');
                refreshAppointments();
            }, 1000);
        }

        function handleUpdateAppointment(e) {
            e.preventDefault();
            
            const updateData = {
                search: document.getElementById('appointment-search').value,
                date: document.getElementById('update-appointment-date').value,
                time: document.getElementById('update-appointment-time').value,
                status: document.getElementById('update-appointment-status').value
            };

            console.log('Updating appointment:', updateData);
            
            // Simulate API call
            setTimeout(() => {
                showNotification('Appointment updated successfully!', 'success');
                closeModal('updateAppointmentModal');
                refreshAppointments();
            }, 1000);
        }

        // Utility functions
        function performQuickSearch(query) {
            if (query.length < 2) return;
            
            console.log('Searching for:', query);
            // Implement search functionality
        }

        function refreshData() {
            showNotification('Refreshing data...', 'info');
            loadDashboardData();
        }

        function refreshContacts() {
            loadRecentContacts();
            updateStats();
        }

        function refreshAppointments() {
            loadTodaysAppointments();
            updateStats();
        }

        function updateStats() {
            // Update the stats cards with fresh data
            // In real implementation, this would make API calls
        }

        function toggleAllSections() {
            allSectionsExpanded = !allSectionsExpanded;
            const toggleText = document.getElementById('toggle-all-text');
            toggleText.textContent = allSectionsExpanded ? 'Collapse All' : 'Expand All';
            
            // Toggle all expandable sections
            // Implementation depends on which sections are expandable
        }

        function showTodaysAppointments() {
            closeAllDropdowns();
            // Scroll to appointments section or show in modal
            document.querySelector('.grid.grid-cols-1.lg\\:grid-cols-2 > div:nth-child(2)').scrollIntoView({ 
                behavior: 'smooth' 
            });
        }

        function editContact(contactId) {
            console.log('Editing contact:', contactId);
            // Open edit modal or navigate to edit page
        }

        function viewContact(contactId) {
            console.log('Viewing contact:', contactId);
            // Open contact details modal
        }

        function editAppointment(appointmentId) {
            console.log('Editing appointment:', appointmentId);
            // Open edit appointment modal
        }

        function startCall(appointmentId) {
            console.log('Starting call for appointment:', appointmentId);
            // Integrate with calling system
        }

        function showNotification(message, type = 'info') {
            // Create and show notification
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 px-4 py-2 rounded-lg text-white transition-all duration-300 ${
                type === 'success' ? 'bg-green-500' : 
                type === 'error' ? 'bg-red-500' : 
                type === 'warning' ? 'bg-yellow-500' : 'bg-blue-500'
            }`;
            notification.textContent = message;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.remove();
            }, 3000);
        }
    </script>
</body>
</html>
