# Logistics — Claude Context

> Sub-project of RinglyPro-CRM. Root `../../CLAUDE.md` applies. Focused context below.

## What this is
Logistics vertical. `backend/` (also a registered additional working directory) + `frontend/`. `uploads/` holds runtime files (ephemeral on Render).

## Deploy
Push to `main` → Render auto-deploy. Build the frontend before pushing UI changes.
