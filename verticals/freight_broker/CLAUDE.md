# Freight Broker — Claude Context

> Sub-project of RinglyPro-CRM. Root `../../CLAUDE.md` applies. Focused context below.

## What this is
Freight broker vertical (loads, lanes, carriers, dispatch). Related to `../cw_carriers` — see that folder's `FREIGHT_BROKER_MASTER_PROMPT.md` for the shared product spec.

## Layout
- `backend/` — API + services
- `frontend/` — UI

## Deploy
Push to `main` → Render auto-deploy. Build the frontend before pushing UI changes.
